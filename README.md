# Kazakh number-to-text tool
Transforms given integer number to kazakh spelling

## Usage
`transform(value: int) -> str`


## Requirements
Project uses `match case` syntax so python version>=3.10 is required
